interface:
  display_name: "Implementation Roadmaps"
  short_description: "Prepare and execute verified roadmap increments"
  default_prompt: "Use $manage-implementation-roadmaps to prepare or execute an approved roadmap continuously through every increment, with focused checks after each increment, final combined standard and end-to-end coverage after all increments are implemented, AI coverage added only for AI-related work, and scope-change and final-approval gates preserved."
