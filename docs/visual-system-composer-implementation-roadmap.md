<!-- Generated by manage-implementation-roadmaps. Use roadmap.py; do not edit manually. -->

# Visual System Composer Implementation Roadmap

- Status: `decision-approval`
- Updated: `2026-07-19T17:07:19Z`

## Objective

Deliver a production-ready visual System Composer that builds on canonical named slots and placements with a mockup-aligned asset and layout palette, safe editable canvas, synchronized properties and layers panels, accessible drag-and-drop, and shared desktop/thin-client behavior.

## Discovery

The completed slot-composer foundations already provide exact workspace asset discovery, required system roots, predefined application/page layouts, canonical named slots and placements, pure add/reorder/reparent/wrap/remove draft operations, protected nodes, bounded undo/redo, schema-generated configuration, typed connections, a keyboard tree, safe recursive preview, and desktop/thin-client parity. The current UI does not yet match the supplied visual-composer mockup: creation controls and layout choices sit above the editor, the asset/canvas/tree workspace exists only in Structure mode, configuration and connections replace the whole workspace, the center is a semantic hierarchy rather than a safe editable visual rendering, and no pointer/touch drag interaction exists. The mockup can therefore be implemented as a shared interaction and presentation layer over the existing canonical draft, with one application-owned layout-remapping seam where changing an existing layout requires deterministic placement mapping. No new persistence graph or renderer-owned system truth is needed.

### Constraints

- Use the existing canonical SystemBuilderRevision, AssetComposition, AssetInstance, AssetPlacement, AssetBinding, exact definitions, and application validators; drag state is transient UI state only.
- The visual canvas must represent predefined named slots and semantic ordering rather than arbitrary coordinates, user-authored dimensions, or renderer-only layout metadata.
- Every pointer or touch drop must resolve to the same pure bounded add, reorder, reparent, wrap, or layout-remap operation used by the complete keyboard and button path.
- The center canvas may render only registered side-effect-free Foundation surfaces and safe fixtures; it must never execute unqualified backing resources, imported code, backend logic, or deployment behavior.
- Desktop and thin client must consume one shared editor shell and one platform client contract with exact workspace context, optimistic save conflicts, and no host-specific composition policy.
- The wide mockup layout should keep palette, canvas, properties, and layers visible; narrower layouts must use accessible drawers or panel tabs without losing function, focus order, or source order.
- Selection, focus, hover, drag overlay, zoom, panel size, and open-panel state are presentation state and must not enter immutable system revisions.
- A layout change for an existing system must be explicit, previewable, cancelable, and deterministic, preserving compatible content and exposing unmatched content rather than silently deleting it.
- Any new drag-and-drop dependency must be version-pinned, license/security reviewed, isolated behind shared UI interaction adapters, and verified in desktop packaging and thin-client builds.
- Keep the old slot-composer roadmap stopped after its verified Increment 3; do not resume its unstarted Increment 4 in parallel with this replacement roadmap.

### Sources

- [System Builder architecture](<architecture/system-builder.md>) - Defines canonical revisions, named slots, ordered placements, shared UI, and non-executing preview boundaries.
- [Current shared System Builder workspace](<../modules/ui/shared/system-builder/SystemBuilderWorkspace.tsx>) - Owns one in-memory draft, save/preview actions, layout gallery, modes, and desktop/thin-client shared behavior.
- [Current semantic structure editor](<../modules/ui/shared/system-builder/SystemComposerStructureEditor.tsx>) - Already contains the palette, slot canvas, synchronized keyboard tree, selection, and explicit structural actions.
- [Canonical draft operations](<../modules/ui/shared/system-builder/systemComposerDraft.ts>) - Pure bounded operations can remain the only commit path for pointer, touch, keyboard, and button interactions.
- [Schema inspector and typed connections](<../modules/ui/shared/system-builder/SystemComposerInspector.tsx>) - Provides the content to reorganize into a persistent properties sidebar without changing system truth.
- [dnd-kit accessibility guide](<https://docs.dndkit.com/guides/accessibility>) - Documents keyboard sensors, tailored instructions, and live-region announcements; it explicitly treats defaults as a starting point rather than complete accessibility.
- [dnd-kit sortable preset](<https://docs.dndkit.com/presets/sortable>) - Provides sortable primitives and spatial keyboard coordinates while leaving canonical state updates to the consumer.
- [WAI-ARIA tree view pattern](<https://www.w3.org/WAI/ARIA/apg/patterns/treeview/>) - Defines the expected expansion, focus, and selection behavior for the persistent Layers and Structure panel.
- [WAI-ARIA toolbar pattern](<https://www.w3.org/WAI/ARIA/apg/patterns/toolbar/>) - Defines grouped editor-toolbar semantics and keyboard focus behavior.
- [WCAG 2.2 reflow](<https://www.w3.org/TR/WCAG22/#reflow>) - Requires retained information and function at narrow equivalent widths while allowing a bounded two-dimensional manipulation region.

## High-level decisions

### Visual composer drag interaction

Which interaction engine should power direct manipulation in the shared visual System Composer?

The mockup requires a persistent asset/layout palette, safe editable canvas, properties sidebar, and layers tree with pointer/touch dragging, while the repository requires an equivalent keyboard path, canonical slot operations, React 19 desktop/thin parity, bounded behavior, and production packaging.

- Status: `awaiting-approval`
- Selected option: `awaiting approval`

| Option | Summary | Recommended | Tradeoffs | Consequences |
| --- | --- | --- | --- | --- |
| dnd-kit canonical adapter (dnd-kit-canonical-adapter) | Use the mature @dnd-kit/core and @dnd-kit/sortable packages only as a shared pointer/touch/keyboard interaction adapter; translate every accepted drop into existing canonical draft commands, with custom slot collision rules, drag handles, overlays, instructions, announcements, and unchanged native action controls. | yes | Adds pinned audited UI dependencies and a modest client bundle cost; Provides maintained pointer, touch, keyboard, overlay, sensor, and sortable foundations; Still requires repository-specific collision, compatibility, focus restoration, announcements, and exhaustive nested-slot tests; Keeps the visual shell declarative and preserves one command/state model across all input methods | Package and lock files change and dependency security/build/package gates become mandatory; A thin shared interaction adapter maps palette insert, within-slot reorder, cross-slot reparent, and layout operations onto pure draft/application commands; The four-region editor shell, responsive drawers, properties tabs, and Layers and Structure tree remain repository-owned shared UI; No drag library type or event crosses into contracts, application, persistence, transports, or immutable revisions |
| Pragmatic DnD adapter (pragmatic-drag-drop-adapter) | Use Atlassian Pragmatic Drag and Drop for pointer direct manipulation and keep keyboard movement in the existing explicit action/menu path with custom live announcements. | no | Smaller framework coupling and strong performance-oriented primitives; Accessibility behavior is intentionally separate from the core package and must be assembled in this repository; Pointer and keyboard paths use different interaction mechanisms even though they converge on the same commands; Nested named-slot collision and drag-preview behavior still require substantial custom code | Package and lock files change and dependency security/build/package gates become mandatory; The editor must provide explicit move menus or buttons as the authoritative keyboard alternative; Shared UI owns live-region messaging, focus restoration, and all semantic tree behavior |
| Repository-owned pointer engine (repository-pointer-engine) | Implement direct manipulation with Pointer Events, geometry measurement, custom overlays, auto-scroll, collision detection, touch activation, keyboard pickup/move/drop, and live announcements without a third-party drag dependency. | no | Avoids a production dependency and gives complete behavior control; Largest implementation, accessibility, browser-compatibility, performance, and maintenance burden; High risk of duplicating mature sensor, cancellation, scrolling, overlay, and focus behavior; Requires broader test fixtures across mouse, touch, pen, keyboard, zoom, and nested scroll containers | The roadmap grows materially and direct-manipulation qualification becomes a larger independent risk boundary; Repository code must own every sensor and accessibility behavior for both Electron and supported browsers; No package changes are needed, but substantially more shared UI infrastructure and maintenance are introduced |

## Increment map

The increment roadmap has not been defined.
